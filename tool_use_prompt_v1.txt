def route_tool(message: str):
    """
    Decide whether an AI assistant should answer a message directly from
    knowledge, or delegate to an external tool.

    Returns one of:
      DIRECT                                          -- answer from knowledge
      {"tool": "web_search",      "args": {"query": "..."}}
      {"tool": "wikipedia_search","args": {"topic": "..."}}
      {"tool": "starcoder",       "args": {"query": "...", "mode": "chat"}}
      {"tool": "starcoder",       "args": {"query": "...", "mode": "complete", "code_prefix": "..."}}
      {"tool": "think",            "args": {"query": "..."}}

    Rules:
      - web_search       for current events, recent news, live data (prices, scores)
      - wikipedia_search for encyclopedic or historical facts
      - starcoder        for any coding task: generation, debugging, explanation, completion
      - think            for complex reasoning, analysis, philosophical/ethical questions, strategic tradeoffs
      - DIRECT           for everything else (math, definitions, greetings, well-known facts)
    """
    ...

route_tool("What is 7 times 8?")
# Returns: DIRECT

route_tool("What is the latest news?")
# Returns: {"tool": "web_search", "args": {"query": "latest news today"}}

route_tool("Who won the Champions League final last weekend?")
# Returns: {"tool": "web_search", "args": {"query": "Champions League final result this weekend"}}

route_tool("Who was Ada Lovelace?")
# Returns: {"tool": "wikipedia_search", "args": {"topic": "Ada Lovelace"}}

route_tool("Write a Python function to binary search a sorted list.")
# Returns: {"tool": "starcoder", "args": {"query": "Write a Python function to binary search a sorted list.", "mode": "chat"}}

route_tool("My script keeps crashing with IndexError: list index out of range.")
# Returns: {"tool": "starcoder", "args": {"query": "My script keeps crashing with IndexError: list index out of range.", "mode": "chat"}}

route_tool("What is the current price of Bitcoin?")
# Returns: {"tool": "web_search", "args": {"query": "Bitcoin price today"}}

route_tool("Hey, how are you doing today?")
# Returns: DIRECT

route_tool("What does the photoelectric effect mean?")
# Returns: DIRECT

route_tool("Complete this Python function for me: def fibonacci(n):")
# Returns: {"tool": "starcoder", "args": {"query": "complete this function", "mode": "complete", "code_prefix": "def fibonacci(n):"}}

route_tool("Can you explain what a transformer model is and show a simple self-attention implementation in Python?")
# Returns: {"tool": "starcoder", "args": {"query": "Explain what a transformer model is and show a simple self-attention implementation in Python.", "mode": "chat"}}

route_tool("What are the latest updates in the Python 3.13 release?")
# Returns: {"tool": "web_search", "args": {"query": "Python 3.13 release updates new features"}}

route_tool("What is the speed of light?")
# Returns: DIRECT

route_tool("What are the philosophical implications of the many-worlds interpretation of quantum mechanics?")
# Returns: {"tool": "think", "args": {"query": "What are the philosophical implications of the many-worlds interpretation of quantum mechanics?"}}

route_tool("Should I use a microservices or monolith architecture for my early-stage startup?")
# Returns: {"tool": "think", "args": {"query": "Should I use a microservices or monolith architecture for my early-stage startup?"}}
